stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - docker-compose build --no-cache
  tags:
    - docker

test:
  stage: test
  script:
    - echo "Starting tests"
  tags:
    - docker

deploy:
  stage: deploy
  script:
    - docker-compose up -d
  tags:
    - docker

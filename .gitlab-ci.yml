stages:
  - build
  - test
  - deploy
services:
  - docker:dind

build:
  stage: build
  image: docker:latest
  script:
    - apk add --no-cache docker-compose
    - docker-compose build --no-cache
  tags:
    - runner

test:
  stage: test
  image: docker:latest
  script:
    - echo "Starting tests"
  tags:
    - runner

deploy:
  stage: deploy
  image: docker:latest
  script:
    - apk add --no-cache docker-compose
    - docker-compose up -d
  tags:
    - runner

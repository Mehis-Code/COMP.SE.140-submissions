stages:
  - build
  - test
  - deploy
services:
  - docker:dind

build:
  stage: build
  image: alpine:latest
  script:
    - apk add --no-cache docker-compose
    - docker-compose build --no-cache
  tags:
    - docker4

test:
  stage: test
  image: alpine:latest
  script:
    - echo "Starting tests"
  tags:
    - docker4

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - apk add --no-cache docker-compose
    - docker-compose up -d
  tags:
    - docker4
